import { createServiceRoleClient } from "@/lib/admin/serviceRole";
export async function recordContentAnalytics(params: { workspaceId: string; entryId: string; versionId?: string | null; destinationUrl: string; source?: string; periodStart: string; periodEnd: string; pageViews?: number; users?: number; engagementSeconds?: number; conversions?: number; metrics?: Record<string, unknown> }) { const { data, error } = await createServiceRoleClient().from("content_analytics_snapshots").upsert({ workspace_id: params.workspaceId, entry_id: params.entryId, version_id: params.versionId ?? null, destination_url: params.destinationUrl, source: params.source ?? "ga4", period_start: params.periodStart, period_end: params.periodEnd, page_views: params.pageViews ?? null, users_count: params.users ?? null, engagement_seconds: params.engagementSeconds ?? null, conversions: params.conversions ?? null, metrics_json: params.metrics ?? {} }, { onConflict: "workspace_id,entry_id,version_id,source,period_start,period_end" }).select().single(); if (error) throw new Error(error.message); return data; }
export async function getEntryAnalytics(workspaceId: string, entryId: string) { const { data } = await createServiceRoleClient().from("content_analytics_snapshots").select("*").eq("workspace_id", workspaceId).eq("entry_id", entryId).order("period_end", { ascending: false }).limit(24); return data ?? []; }
