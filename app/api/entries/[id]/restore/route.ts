import { NextResponse } from "next/server";
import { requirePlatformAccess } from "@/lib/platform/permissions";
import { restoreEntryVersion } from "@/lib/content/entryService";
import { canActorOperateEntry } from "@/lib/schema/modelAccess";
export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) { const auth = await requirePlatformAccess(req, { permission: "content.entry.edit" }); if (auth.error) return auth.error; try { const { id } = await params; const access = await canActorOperateEntry(auth.data!.actor, id, "edit"); if (!access.allowed) return NextResponse.json({ success: false, data: null, error: access.error, timestamp: new Date().toISOString() }, { status: access.status }); const { versionId } = await req.json(); const result = await restoreEntryVersion({ workspaceId: auth.data!.actor.workspaceId, entryId: id, versionId, actorAdminUserId: auth.data!.actor.adminUserId }); return NextResponse.json({ success: result.ok, data: result.ok ? result.data : null, error: result.ok ? null : result.error, timestamp: new Date().toISOString() }, { status: result.ok ? 201 : result.status }); } catch (error) { return NextResponse.json({ success: false, data: null, error: error instanceof Error ? error.message : "Invalid restore request", timestamp: new Date().toISOString() }, { status: 400 }); } }
