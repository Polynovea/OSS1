import { requireDeveloperApi } from "@/lib/developer/apiAuth";
import { apiError, apiSuccess } from "@/lib/developer/apiContract";
import { planSchemaBundle } from "@/lib/developer/schemaDeliveryService";

export async function POST(req:Request,{params}:{params:Promise<{model:string}>}){const {model:apiKey}=await params;const auth=await requireDeveloperApi(req,{scope:"schema.read",modelApiKey:apiKey});if(auth.error)return auth.error;try{const body=await req.json();if(!body.schema||body.schema.apiKey!==apiKey)return apiError("SCHEMA_API_KEY_MISMATCH","schema.apiKey must match the route model",400,{requestId:auth.data!.requestId,rateLimit:auth.data!.rateLimit});const bundle={format:"polynovea-cms-schema-bundle",formatVersion:1,generatedAt:new Date().toISOString(),models:[{apiKey,name:body.schema.name||apiKey,version:1,schemaHash:"",schema:body.schema}]};const plan=await planSchemaBundle(auth.data!.workspaceId,bundle);return apiSuccess(plan,{requestId:auth.data!.requestId,rateLimit:auth.data!.rateLimit});}catch(e){return apiError("SCHEMA_DIFF_FAILED",e instanceof Error?e.message:"Could not diff schema",400,{requestId:auth.data!.requestId,rateLimit:auth.data!.rateLimit});}}
